# Sam-and-substrings-ALGO

This function calculates the sum of all numeric substrings of a string n modulo (10^9+7). It builds the contribution of each digit iteratively, multiplying by its position, so every substring ending at that digit is counted efficiently.
